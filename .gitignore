# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
*.local.*
jscpd-report

# Environment variables (contains Sentry DSN — must never be committed)
.env
.env.local
.env.*.local

# Test coverage reports
coverage/
# tsc project references write build info next to the root configs
*.tsbuildinfo
# Rust LLVM coverage artifacts (cargo-llvm-cov)
src-tauri/target/llvm-cov/

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

# Bundle analysis
bundle-analysis.html

# CodeGraph index (local code intelligence database)
.codegraph/

# Trae editor "share HTML" snapshots (duplicates of tracked entry points)
.trae-html-share-packages/

# Editor directories and files
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
